<% 

  posted_params = request.fullpath.split("/")

  if posted_params[1].to_s == posted_params[3].to_s
%>
<%# comment on comment %>
$div = "#replies-" + <%= params[:comment_id] %>
$($div).append("<%= j (render "shared/comment_form", comment: @comment, commentable: @commentable)%>")
<% else %>
<%# comment on post %>

<% end %>